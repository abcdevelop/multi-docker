language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t $DOCKER_ID/react-test -f ./client/Dockerfile.dev ./client/.

script:
  #- docker run abcdevelop/react-test yarn test --coverage
  - docker run -e CI=true $DOCKER_ID/react-test yarn test

after_success:
  - docker build -t $DOCKER_ID/multi-nginx ./nginx
  - docker build -t $DOCKER_ID/multi-server ./server
  - docker build -t $DOCKER_ID/multi-worker ./worker
  - docker build -t $DOCKER_ID/multi-client ./client
  # Log in to the docker CLI
  #- echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  #- echo $DOCKER_PASSWORD | docker login -u abcdevelop --password-stdin
  - echo $DOCKER_PASSWORD | docker login -u $DOCKER_ID --password-stdin
  # Take those images and push them to docker hub
  - docker push $DOCKER_ID/multi-nginx
  - docker push $DOCKER_ID/multi-server
  - docker push $DOCKER_ID/multi-worker
  - docker push $DOCKER_ID/multi-client


#deploy:
#  provider: elasticbeanstalk
#  region: "us-east-1"
#  app: "react-test"
#  env: "DockerReact-env"
#  bucket_name: "elasticbeanstalk-us-east-1-169521840923"
#  bucket_path: "react-test"
#  on:
#    branch: master
#access_key_id: "$AWS_ACCESS_KEY"
#secret_access_key: 
#  secure: "$AWS_SECRET_KEY"
#access_key_id: 
#  secure: $AWS_ACCESS_KEY
#secret_access_key:
#  secure: $AWS_SECRET_KEY
#access_key_id: 
  #secure: $AWS_ACCESS_KEY